---
# Deply frontend build to servers.
- hosts: instance_1
  vars_files:
    - host_vars/general_vars.yaml
    - host_vars/vault.yaml
  gather_facts: true
  remote_user: ubuntu
  become: yes
  become_method: sudo
  roles:
    - config_watchtower
    - config_letsencrypt
    - docker_client
    - slack_notification

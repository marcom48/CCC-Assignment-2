{
    "_id": "_design/user_stats",
    "views": {
        "get_suburb_sentiment": {
            "map": "function (doc) {var dt = new Date(doc.created_at); emit([doc.location.suburb, dt.getFullYear(), dt.getMonth(), dt.getDate(), doc.sentiment], 1)}",
            "reduce": "_count"
        }
    }
}